PLAIN mode demo — plain-folder-demo / Plain Folder Demo
===============================

Listed in .upriv/state.json as storage_mode=plain, security_mode=disk_open_close (not encrypted_dir / RAM store).

Files here are LITERAL plaintext on the HD/SSD:
  workspace/Plain Folder Demo/

Flow:
  open  → extract Plain Folder Demo.7z to this folder (plaintext)
  edit  → saves stay here in clear until seal
  seal  → new .7z + secure_wipe_workspace + remove this tree

Local password (HD, not RAM-only):
  .upriv/vaults/plain-folder-demo/auth/.session.enc
  .upriv/vaults/plain-folder-demo/auth/.quick-auth

No encrypted store:
  (no .upriv/vaults/plain-folder-demo/store/)
