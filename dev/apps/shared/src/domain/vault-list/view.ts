/** Matches `[ui].vault_list_view` in settings.toml (wire: `settings.ui.vault_list_view`). */
export type VaultListViewMode = "default" | "large" | "compact" | "blocks";

export const DEFAULT_VAULT_LIST_VIEW: VaultListViewMode = "default";
