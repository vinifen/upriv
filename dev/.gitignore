# Dependencies (per-app: desktop/node_modules, mobile/node_modules — never dev/node_modules)
node_modules/

# Frontend build
desktop/dist/
mobile/.expo/
mobile/dist/
mobile/web-build/

# Rust (workspace + legacy per-crate)
target/
src-tauri/target/

# Tauri generated (regenerated on build)
src-tauri/gen/

# Logs / OS
*.log
.DS_Store
Thumbs.db

# Env (never commit secrets)
.env
.env.*
